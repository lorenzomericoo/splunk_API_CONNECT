[launcher]
author=API Connect
description=Wizard GUI stile Postman per la creazione di Modular Input Splunk. Chain Builder visuale, pipeline di trasformazioni per-campo, output format configurabile, circuit breaker, token cache OAuth2, metriche per-run.
version=3.0.0

[package]
id=api_connect
check_for_updates=false

[ui]
is_visible=true
label=API Connect

[install]
is_configured=false
build=3
