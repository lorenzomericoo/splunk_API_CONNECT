[launcher]
author=API Connect
description=Wizard GUI stile Postman per la creazione di Modular Input Splunk che interrogano API REST esterne. Chain Builder visuale con auth per-call, join tra endpoint, error policy e mapping al tracciato standard aziendale.
version=2.0.0

[package]
id=api_connect
check_for_updates=false

[ui]
is_visible=true
label=API Connect

[install]
is_configured=false
build=2
