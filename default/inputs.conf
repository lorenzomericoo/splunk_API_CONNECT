# inputs.conf — API Connect
# Le stanze degli input generati vengono aggiunte automaticamente
# dal GenerateInputHandler via REST.
# Questo file contiene solo la dichiarazione dello schema.

[script]
disabled = false
